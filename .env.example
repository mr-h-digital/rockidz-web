# Local dev — points at your Spring Boot app running locally
VITE_API_BASE_URL=http://localhost:8080

# Production — set this in your GitHub Actions build step (or a local
# .env.production file) once the Railway backend is deployed:
# VITE_API_BASE_URL=https://rockidz-api.rockmission.co.za
